import React from 'react';
import './LoginPage.scss';
import '../../App.scss';

const LoginPage = () => {

    return (
        <div>
            The view for a LOGIN page
        </div>
    );
};

export default LoginPage;